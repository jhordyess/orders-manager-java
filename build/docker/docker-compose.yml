services:
  mariadb:
    image: mariadb:10.4
    restart: always
    environment:
      - MARIADB_ROOT_PASSWORD
      - MARIADB_DATABASE
    volumes:
      - ./backup.sql:/docker-entrypoint-initdb.d/backup.sql
  tomcat:
    depends_on:
      - mariadb
    build:
      context: .
      dockerfile: ./tomcat.dockerfile
    restart: always
    environment:
      - DB_HOST=mariadb
      - DB_USER
      - DB_PASSWORD
      - DB_NAME
    ports:
      - "8080:8080"
